# commando-standard

Standard library of reusable Commando command modules.

## Usage

In `.commando/config.yml`:

```yaml
dependencies:
  - github:jdillon/commando-standard

modules:
  - "@planet57/commando-standard/hello"
```

## Modules

- **hello** - Example module with `greet` and `info` commands
